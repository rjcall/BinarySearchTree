Program creates a binary search tree with functions:

  insert(int)

  erase(int)

  clear() // which deletes all nodes in tree & handles the dynamic memory

Output functions:

  print() // indents the output of the node data according to its depth in the tree

  Tree Traversals:

    // traverses tree and outputs in order of that traversal

    in-order()
    pre-order()
    post-order()


Interactive driver
